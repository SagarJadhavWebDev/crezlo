{
  "name": "crezlo",
  "version": "1.0.0",
  "main": "index.js",
  "private":true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run build --workspace=core",
    "dev": "npm run dev --workspace=core-preview",
    "release":"${npm_package_version}"
  },
  "workspaces":["core","core-preview"],
  "author": "",
  "license": "ISC",
  "description": ""
}

