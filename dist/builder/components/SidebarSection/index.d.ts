import { ReactNode } from "react";
export declare const SidebarSection: ({ children, title, background, showBreadcrumbs, noBorderTop, noPadding, isLoading, pageOptions, }: {
    children: ReactNode;
    title: ReactNode;
    background?: string;
    showBreadcrumbs?: boolean;
    noBorderTop?: boolean;
    noPadding?: boolean;
    isLoading?: boolean | null;
    pageOptions?: ReactNode;
}) => import("react/jsx-runtime").JSX.Element;
