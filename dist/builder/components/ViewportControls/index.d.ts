import { Viewport } from "../../types";
export declare const ViewportControls: ({ autoZoom, zoom, onViewportChange, onZoom, }: {
    autoZoom: number;
    zoom: number;
    onViewportChange: (viewport: Viewport) => void;
    onZoom: (zoom: number) => void;
}) => import("react/jsx-runtime").JSX.Element;
